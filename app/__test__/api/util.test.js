// const { urlMaker } = require("../../src/utils");

describe("Utils test", () => {
  test("should return random url", () => {
    expect(1 + 1).toBe(2);
    // const url = urlMaker();
    // expect(url).stringContaining("-");
  });
});
